<Window
    x:Class="WeatherClient.Views.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:vm="using:WeatherClient.ViewModels"
    xmlns:models="using:WeatherClient.Models"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:scott="using:ScottPlot.WinUI"
    mc:Ignorable="d"
    Title="Погода 2.0">

    <NavigationView x:Name="NavView"
                    PaneDisplayMode="Top"
                    IsSettingsVisible="False"
                    SelectionChanged="NavView_SelectionChanged"
                    Loaded="NavView_Loaded"
                    Background="#FFEDF2F7">

        <NavigationView.MenuItems>
            <NavigationViewItem Icon="World" Content="Сейчас" Tag="Current" IsSelected="True"/>
            <NavigationViewItem Icon="Calendar" Content="Прогноз на 5 дней" Tag="Forecast"/>
            <NavigationViewItem Icon="ShowResults" Content="Графики" Tag="Charts"/>
        </NavigationView.MenuItems>

        <NavigationView.PaneHeader>
            <StackPanel Orientation="Horizontal" Spacing="15" Margin="20,10,20,0">
                <TextBox Width="250" PlaceholderText="Введите город" Text="{Binding City, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" CornerRadius="8" Padding="12,8"/>
                <Button Command="{Binding GetWeatherCommand}" CornerRadius="8" Background="#FF2196F3" Foreground="White" ToolTipService.ToolTip="Обновить">
                    <FontIcon Glyph="&#xE72C;" FontFamily="Segoe MDL2 Assets" />
                </Button>
            </StackPanel>
        </NavigationView.PaneHeader>

        <Frame x:Name="ContentFrame" Padding="20">
            <Frame.ContentTransitions>
                <TransitionCollection>
                    <NavigationThemeTransition/>
                </TransitionCollection>
            </Frame.ContentTransitions>
        </Frame>

        <NavigationView.Resources>
            <DataTemplate x:Key="CurrentWeatherTemplate">
                <Border Background="#FFFFFF" BorderBrush="#FFBBDEFB" BorderThickness="1" CornerRadius="16" Padding="25" VerticalAlignment="Top" MaxWidth="450" HorizontalAlignment="Center">
                    <StackPanel HorizontalAlignment="Center" Spacing="25">
                        <Image Source="{Binding IconUrl}" Height="140" Width="140" HorizontalAlignment="Center" Margin="0,10,0,0"/>
                        <StackPanel Orientation="Horizontal" Spacing="15" HorizontalAlignment="Center">
                            <TextBlock Text="{Binding Temperature}" FontSize="72" FontWeight="Light" Foreground="#FF2196F3" VerticalAlignment="Center" Margin="0,-10,0,0"/>
                            <StackPanel VerticalAlignment="Center" Spacing="5">
                                <TextBlock Text="{Binding City}" FontSize="32" FontWeight="SemiBold" Foreground="#FF212121" TextTrimming="CharacterEllipsis" MaxWidth="200"/>
                                <TextBlock Text="{Binding Description}" FontSize="18" Foreground="#FF616161"/>
                            </StackPanel>
                        </StackPanel>
                        <Rectangle Height="1" Fill="#FFEEEEEE" Margin="0,15,0,15"/>
                        <Grid ColumnSpacing="40" RowSpacing="25">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                            </Grid.RowDefinitions>

                            <StackPanel Grid.Column="0" Grid.Row="0">
                                <StackPanel Orientation="Horizontal" Spacing="5">
                                    <FontIcon Glyph="&#xE7BE;" FontFamily="Segoe MDL2 Assets" FontSize="16" Foreground="#FF2196F3"/>
                                    <TextBlock Text="Влажность" FontSize="15" FontWeight="SemiBold" Foreground="#FF424242"/>
                                </StackPanel>
                                <TextBlock Text="{Binding Humidity}" FontSize="20" FontWeight="Medium" Margin="0,5,0,0"/>
                            </StackPanel>
                            <StackPanel Grid.Column="1" Grid.Row="0">
                                <StackPanel Orientation="Horizontal" Spacing="5">
                                    <FontIcon Glyph="&#xE715;" FontFamily="Segoe MDL2 Assets" FontSize="16" Foreground="#FF2196F3"/>
                                    <TextBlock Text="Давление" FontSize="15" FontWeight="SemiBold" Foreground="#FF424242"/>
                                </StackPanel>
                                <TextBlock Text="{Binding Pressure}" FontSize="20" FontWeight="Medium" Margin="0,5,0,0"/>
                            </StackPanel>
                            <StackPanel Grid.Column="0" Grid.Row="1">
                                <StackPanel Orientation="Horizontal" Spacing="5">
                                    <FontIcon Glyph="&#xE82D;" FontFamily="Segoe MDL2 Assets" FontSize="16" Foreground="#FF2196F3"/>
                                    <TextBlock Text="Ветер" FontSize="15" FontWeight="SemiBold" Foreground="#FF424242"/>
                                </StackPanel>
                                <TextBlock Text="{Binding WindSpeed}" FontSize="20" FontWeight="Medium" Margin="0,5,0,0"/>
                            </StackPanel>
                            <StackPanel Grid.Column="1" Grid.Row="1">
                                <StackPanel Orientation="Horizontal" Spacing="5">
                                    <FontIcon Glyph="&#xEC4A;" FontFamily="Segoe MDL2 Assets" FontSize="16" Foreground="#FF2196F3"/>
                                    <TextBlock Text="Восход" FontSize="15" FontWeight="SemiBold" Foreground="#FF424242"/>
                                </StackPanel>
                                <TextBlock Text="{Binding Sunrise}" FontSize="20" FontWeight="Medium" Margin="0,5,0,0"/>
                            </StackPanel>
                            <StackPanel Grid.Column="1" Grid.Row="2">
                                <StackPanel Orientation="Horizontal" Spacing="5">
                                    <FontIcon Glyph="&#xEC4B;" FontFamily="Segoe MDL2 Assets" FontSize="16" Foreground="#FF2196F3"/>
                                    <TextBlock Text="Закат" FontSize="15" FontWeight="SemiBold" Foreground="#FF424242"/>
                                </StackPanel>
                                <TextBlock Text="{Binding Sunset}" FontSize="20" FontWeight="Medium" Margin="0,5,0,0"/>
                            </StackPanel>
                        </Grid>
                    </StackPanel>
                </Border>
            </DataTemplate>

            <DataTemplate x:Key="ForecastTemplate">
                <ScrollViewer HorizontalScrollBarVisibility="Auto" VerticalScrollBarVisibility="Disabled">
                    <ItemsControl ItemsSource="{Binding DailyForecasts}">
                        <ItemsControl.ItemsPanel>
                            <ItemsPanelTemplate>
                                <StackPanel Orientation="Horizontal" Spacing="15" HorizontalAlignment="Center"/>
                            </ItemsPanelTemplate>
                        </ItemsControl.ItemsPanel>
                        <ItemsControl.ItemTemplate>
                            <DataTemplate x:DataType="models:DailyForecastUiModel">
                                <Border Background="White" CornerRadius="12" Width="130" Padding="15" BorderBrush="#FFBBDEFB" BorderThickness="1">
                                    <StackPanel HorizontalAlignment="Center" Spacing="8">
                                        <TextBlock Text="{x:Bind DayName}" FontSize="18" FontWeight="Bold" Foreground="#FF2196F3" HorizontalAlignment="Center"/>
                                        <TextBlock Text="{x:Bind DateDisplay}" FontSize="14" Foreground="Gray" HorizontalAlignment="Center"/>
                                        <Image Source="{x:Bind IconUrl}" Width="60" Height="60"/>
                                        <TextBlock Text="{x:Bind Description}" FontSize="12" TextWrapping="Wrap" TextAlignment="Center" Height="32"/>
                                        <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Spacing="10" Margin="0,10,0,0">
                                            <StackPanel>
                                                <TextBlock Text="Макс" FontSize="10" Foreground="Gray"/>
                                                <TextBlock Text="{x:Bind MaxTempStr}" FontSize="20" FontWeight="SemiBold"/>
                                            </StackPanel>
                                            <Rectangle Width="1" Fill="#EEEEEE"/>
                                            <StackPanel>
                                                <TextBlock Text="Мин" FontSize="10" Foreground="Gray"/>
                                                <TextBlock Text="{x:Bind MinTempStr}" FontSize="20" Foreground="Gray"/>
                                            </StackPanel>
                                        </StackPanel>
                                    </StackPanel>
                                </Border>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </ScrollViewer>
            </DataTemplate>

            <DataTemplate x:Key="ChartsTemplate">
                <Border Background="White" CornerRadius="12" Padding="20" BorderBrush="#FFBBDEFB" BorderThickness="1" Margin="0,20,0,0">
                    <scott:WinUIPlot x:Name="TemperatureChart" Height="400"/>
                </Border>
            </DataTemplate>

            <ContentControl x:Key="ContentPresenter" Content="{Binding}"/>
        </NavigationView.Resources>
    </NavigationView>
</Window>